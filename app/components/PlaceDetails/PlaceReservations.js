"use client";
import { InitialDateRange } from "@/app/config/constants";
export function PlaceReservations(props) {
  const { listDetails, currentUser } = props;
  return <div>PlaceReservations</div>;
}
